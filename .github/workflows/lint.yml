name: Lint

on:
    workflow_call:

jobs:
    lint:
        runs-on: ubuntu-latest

        steps:
            - name: Setup Go
              uses: actions/setup-go@v4
              with:
                  go-version: "1.20"

            - name: Pre Commit
              uses: pre-commit/action@v3.0.0
